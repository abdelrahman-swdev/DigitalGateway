namespace DigitalGateway.Core.Errors
{
    public enum CommonErrorCodes
    {
        NULL,
        OPERATION_FAILED,
        INVALID_INPUT,
        SERVER_ERROR,
        INVALID_EMAIL_OR_PASSWORD,
        NOT_AUTHORIZED,
        NOT_FOUND,
    }
}
